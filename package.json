{
  "name": "mcp-registry",
  "version": "0.1.0",
  "description": "Registry of MCP package metadata",
  "main": "build-index.js",
  "scripts": {
    "validate": "node validate.js",
    "build": "node build-index.js"
  },
  "dependencies": {
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1"
  }
}